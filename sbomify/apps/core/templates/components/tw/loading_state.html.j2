{% comment %}Legacy include adapter. Content loading has one implementation in the Cotton library. Old row/size/compact options no longer select a brand spinner.{% endcomment %}
<div {{ attrs|safe }}>
    <c-feedback.loading message="{{ message|default:'Loading content...' }}" class="{{ class|default:'' }}" />
</div>
