{% if label or label_text %}
    <div class="tw-progress-header">
        <span>{{ label_text|default:"Progress" }}</span>
        <span class="tw-progress-value">{{ value }}%</span>
    </div>
{% endif %}
<div class="tw-progress{% if size %} tw-progress-{{ size }}{% endif %}{% if class %} {{ class }}{% endif %}"
     role="progressbar"
     aria-valuenow="{{ value }}"
     aria-valuemin="0"
     aria-valuemax="100"
     {% if label_text %}aria-label="{{ label_text }}"{% endif %}>
    <div class="tw-progress-bar tw-progress-bar-{{ variant|default:'primary' }}"
         style="width: {{ value }}%"></div>
</div>
