{% comment %}Legacy include adapter to the margin-free shared skeleton primitive.{% endcomment %}
<c-feedback.skeleton type="{{ type|default:'text' }}" lines="{{ lines|default:3 }}" width="{{ width|default:'' }}" height="{{ height|default:'' }}" class="{{ class|default:'' }}" />
