<label class="tw-toggle-label{% if class %} {{ class }}{% endif %}">
    <input type="checkbox"
           id="{{ name }}"
           name="{{ name }}"
           class="tw-toggle"
           {% if checked %}checked{% endif %}
           {% if disabled %}disabled{% endif %}
           {{ attrs|safe }}>
    <span>{{ label }}</span>
</label>
