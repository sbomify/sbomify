{% comment %}
PublicCard Component - A wrapper around StandardCard for public-facing pages

Usage:
{% include 'core/components/public_card.html.j2' with {
    title: 'Card Title',
    subtitle: 'Optional subtitle',
    icon: 'fas fa-info-circle',
    variant: 'primary',
    size: 'md',
    hoverable: true,
    padded: true
} %}

Parameters:
- title: Card title (optional)
- subtitle: Subtitle text shown as info notice (optional)
- icon: Icon class (default: 'fas fa-info-circle')
- variant: 'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info'
- size: 'sm' | 'md' | 'lg'
- hoverable: Enable hover effect (default: false)
- padded: Add body padding (default: true)
{% endcomment %}
<div class="mt-4 {% if hoverable %}hover:shadow-md transition-shadow{% endif %}">
    <div class="tw-card shadow-sm">
        {% if title or header_content %}
            <div class="px-6 py-4 border-b border-border">
                <div class="flex items-center justify-between gap-4">
                    {% if title %}<h4 class="text-lg font-semibold text-text m-0">{{ title }}</h4>{% endif %}
                    {% if header_content %}<div class="flex items-center gap-2">{{ header_content }}</div>{% endif %}
                </div>
            </div>
        {% endif %}
        <div class="p-6">
            {% if subtitle %}
                <div class="flex items-center gap-2 mb-4 p-3 rounded-lg bg-info/10">
                    <i class="{{ icon|default:'fas fa-info-circle' }} text-primary"></i>
                    <span class="text-text-muted text-sm">{{ subtitle }}</span>
                </div>
            {% endif %}
            {{ content }}
        </div>
        {% if footer_content %}<div class="px-6 py-4 border-t border-border">{{ footer_content }}</div>{% endif %}
    </div>
</div>
