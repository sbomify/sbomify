{% extends "core/emails/base.html.j2" %}
{% block title %}Your Access Request Has Been Approved{% endblock %}
{% block content %}
    <h1>Access Request Approved</h1>
    <p>
        Dear
        {% if user.first_name %}
            {{ user.first_name }}
        {% else %}
            user
        {% endif %}
        ,
    </p>
    <p>
        Your request for access to gated components in the workspace <strong>{{ team.name }}</strong> has been approved.
    </p>
    <p>
        You can now access gated documents and SBOMs in this workspace. Click the button below to log in and view the Trust Center.
    </p>
    <p>
        <a href="{{ login_link }}" class="button">Log In to Access</a>
    </p>
    <p class="text-secondary">If you cannot click the button above, copy and paste this link into your browser:</p>
    <p class="text-secondary">{{ login_link }}</p>
    <p>
        Regards,
        <br>
        The sbomify Team
    </p>
{% endblock %}
