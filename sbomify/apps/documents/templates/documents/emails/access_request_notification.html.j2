{% extends "core/emails/base.html.j2" %}
{% block title %}New Access Request{% endblock %}
{% block content %}
    <h1>New Access Request</h1>
    {# djlint:off #}
    <p>Dear {% if admin_user.first_name %}{{ admin_user.first_name }}{% else %}administrator{% endif %},</p>
    {# djlint:on #}
    <p>
        A new access request has been submitted for the workspace <strong>{{ team.name }}</strong>.
    </p>
    <p>
        <strong>Request Details:</strong>
    </p>
    <ul>
        <li>
            <strong>Requester:</strong> {{ requester_name }} ({{ requester_email }})
        </li>
        <li>
            <strong>Workspace:</strong> {{ team.name }}
        </li>
        <li>
            <strong>Requested At:</strong> {{ requested_at }}
        </li>
        {% if requires_nda %}
            <li>
                <strong>NDA Status:</strong>
                {% if nda_signed %}
                    Signed
                {% else %}
                    Pending signature
                {% endif %}
            </li>
        {% endif %}
    </ul>
    <p>Click the button below to review and approve or reject this request.</p>
    <p>
        <a href="{{ review_link }}" class="button">Review Access Request</a>
    </p>
    <p class="text-secondary">If you cannot click the button above, copy and paste this link into your browser:</p>
    <p class="text-secondary">{{ review_link }}</p>
    <p>
        Regards,
        <br>
        The sbomify Team
    </p>
{% endblock %}
