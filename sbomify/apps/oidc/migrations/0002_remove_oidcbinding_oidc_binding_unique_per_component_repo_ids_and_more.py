# Generated by Django 5.2.14 on 2026-06-02 12:10

from django.conf import settings
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("oidc", "0001_initial"),
        ("sboms", "0064_alter_component_is_global"),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.RemoveConstraint(
            model_name="oidcbinding",
            name="oidc_binding_unique_per_component_repo_ids",
        ),
        migrations.AlterField(
            model_name="oidcbinding",
            name="repository",
            field=models.CharField(
                help_text="External repository identifier. GitHub: 'org/repo' (case-insensitive, stored lowercase). Used to match a still-UNPINNED binding on the first exchange; once the IDs below are pinned, matching is by ID and this string is display-only (may go stale after a rename).",
                max_length=255,
            ),
        ),
        migrations.AlterField(
            model_name="oidcbinding",
            name="repository_id",
            field=models.BigIntegerField(
                blank=True,
                help_text="GitHub's immutable numeric repository ID. NULL until pinned. Resolved at create time for public repos; for private repos (sbomify can't read their metadata anonymously) it stays NULL and is pinned from the first signed OIDC token at exchange. Once set, the exchange matches on this, NEVER the name.",
                null=True,
            ),
        ),
        migrations.AlterField(
            model_name="oidcbinding",
            name="repository_owner_id",
            field=models.BigIntegerField(
                blank=True,
                help_text="GitHub's immutable numeric owner (user/org) ID. NULL until pinned (see ``repository_id``). Matched alongside ``repository_id`` to defeat account-resurrection attacks.",
                null=True,
            ),
        ),
        migrations.AddConstraint(
            model_name="oidcbinding",
            constraint=models.UniqueConstraint(
                fields=("component", "provider", "repository"),
                name="oidc_binding_unique_per_component_repo",
            ),
        ),
    ]
