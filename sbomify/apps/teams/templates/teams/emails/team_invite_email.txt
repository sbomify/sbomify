Dear invitee,

{% if user.get_full_name %}{{ user.get_full_name }}{% else %}{{ user.email }}{% endif %} has invited you to join the workspace {{ team.name }} on sbomify as {{ invitation.role }}.

Please use the link below to accept the invitation:

{{ base_url }}{% url 'teams:accept_invite' invitation.token %}

This invitation will expire on {{ invitation.expires_at|date:"F d, Y" }}.

Regards,
The sbomify Team

─────────────────────────────
Need help? Visit {{ base_url }}/support/contact/
