{% if user.get_full_name %}{{ user.get_full_name }}{% else %}{{ user.email }}{% endif %} has invited you to access the {{ team.name }} Trust Center on sbomify.

After accepting, you may be asked to create an account or sign an NDA. Once that's done, you'll have access to gated components and documents.

Accept the invitation: {{ base_url }}{% url 'teams:accept_invite' invitation.token %}

This invitation expires on {{ invitation.expires_at|date:"F d, Y" }}.

Cheers,
The sbomify Team

Questions? We're here to help: {{ base_url }}/support/contact/
