<div class="flex flex-col gap-5"
     x-data="accountDangerZone({ csrfToken: '{{ csrf_token }}', tokenCount: {{ access_token_count }} })">
    <c-layout.section-header level="2" title="Account" subtitle="Your sign-in details and personal preferences." />
    <c-cards.card flush>
    <c-cards.header compact title="You" />
    <c-forms.field horizontal label="Name" hint="Your name comes from your sign-in provider.">
    <c-forms.input size="sm" readonly aria-label="Name" value="{{ request.user.get_full_name|default:request.user.username }}" />
    </c-forms.field>
    <c-forms.field horizontal label="Email" hint="Used to sign in and receive notifications.">
    <c-forms.input size="sm" readonly aria-label="Email" value="{{ request.user.email }}" />
    </c-forms.field>
    <c-forms.field horizontal label="Sign-in" hint="Manage your password and two-factor authentication with your sign-in provider.">
    <c-forms.hint>Managed by your identity provider</c-forms.hint>
    </c-forms.field>
    </c-cards.card>
    <c-cards.card flush>
    <c-cards.header compact title="Appearance" />
    <c-forms.field horizontal label="Theme" hint="Choose a light or dark appearance, or follow your system setting.">
    <c-chrome.theme-picker standalone />
    </c-forms.field>
    </c-cards.card>
    <c-cards.dangerzone-collapsible>
    <c-layout.section-header title="Delete account" subtitle="Your account is deactivated immediately and deleted for good after 14 days.">
    <c-slot name="actions"><c-buttons.danger size="sm" @click.prevent="openDeleteModal()" aria-label="Delete your account">Delete account</c-buttons.danger></c-slot>
    </c-layout.section-header>
    </c-cards.dangerzone-collapsible>
    {# Delete Account Modal #}
    <c-modal id="deleteAccountModal" show="showDeleteModal" title="Delete your account" icon="fas fa-exclamation-triangle" role="alertdialog" aria-describedby="deleteAccountModalDesc">
    <c-forms.hint id="deleteAccountModalDesc" class="mb-4">
    Your account is deactivated immediately and deleted for good after <strong>14 days</strong>.
    </c-forms.hint>
    <c-feedback.alert variant="warning" icon="fas fa-key" class="mb-4" x-show="tokenCount > 0" x-cloak>
    You have <strong x-text="tokenCount"></strong> active API <span x-text="tokenCount === 1 ? 'token' : 'tokens'"></span> that will be <strong>immediately revoked</strong>. Any CI/CD pipelines using <span x-text="tokenCount === 1 ? 'it' : 'them'"></span> will stop working.
    </c-feedback.alert>
    <c-feedback.alert variant="info" title="Immediate effects" class="mb-4">
    <ul class="pl-4 space-y-1">
        <li>Account deactivated (cannot sign in)</li>
        <li>Personal access tokens revoked</li>
        <li>Pending invitations removed</li>
        <li>
            Workspaces where you are the sole member will be deleted, <strong>including all their products, SBOMs, and documents</strong>
        </li>
        <li>Stripe subscriptions for deleted workspaces will be cancelled</li>
    </ul>
    </c-feedback.alert>
    <c-feedback.alert variant="info" icon="fas fa-clock" title="After 14 days" class="mb-4">
    <ul class="pl-4 space-y-1">
        <li>Account deleted for good</li>
    </ul>
    </c-feedback.alert>
    {% comment %}
        The way back sits outside the "After 14 days" list. It used to be the second
        bullet there, which filed the only recovery instruction under the heading
        for the moment recovery stops being possible.
    {% endcomment %}
    <c-forms.hint class="mb-4">Changed your mind? Contact support within the 14 days and we will stop the deletion.</c-forms.hint>
    {# Validation Error Display #}
    <c-feedback.alert variant="danger" class="mb-4" x-show="validationError" x-cloak>
    <span x-text="validationError"></span>
    </c-feedback.alert>
    <div class="mt-4">
        <c-forms.label for="delete-account-confirm">Type <strong>delete</strong> to confirm:</c-forms.label>
        <c-forms.input id="delete-account-confirm" placeholder="Type 'delete' to confirm" autocomplete="off" x-model="confirmText" ::disabled="isDeleting" @keydown.enter="if (canConfirm && !isDeleting) deleteAccount()" />
    </div>
    <c-slot name="footer">
    <c-buttons.ghost @click="closeModal()" ::disabled="isDeleting">Cancel</c-buttons.ghost>
    <c-buttons.danger @click="deleteAccount()" ::disabled="!canConfirm || isDeleting">
    <c-feedback.spinner size="sm" x-show="isDeleting" />
    <i x-show="!isDeleting" class="fas fa-trash"></i>
    <span x-text="isDeleting ? 'Deleting…' : 'Delete my account'"></span>
    </c-buttons.danger>
    </c-slot>
    </c-modal>
</div>
